---
name: genesis
description: Build a new user's profile. Trigger on /genesis, or automatically when /stress-test finds profile/ empty or missing (new user, fresh kit, handed-over system). Runs ONCE per user. NOT for daily use.
---

# /genesis — thin pointer

The logic lives in the kernel, not here.

1. **Locate ROOT:** among the mounted folders, find the one containing `kernel/CLAUDE-KERNEL.md`.
2. **Read `kernel/PROFILE-GENESIS.md` fully.**
3. **Execute it exactly as written:** three openers → friction diagnosis (uncover, never assume) → adoption red line → data-prompt + first-win scoping → quick-fire practicals → write the profile files live from `profile-templates/`, read back, tag everything self-reported → flow into /new-project.
4. Plain language throughout — the user hasn't read the glossary yet. Target ~15 minutes, hard stop 25.
